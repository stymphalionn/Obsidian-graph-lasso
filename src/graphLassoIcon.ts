/** Ribbon icon now uses built-in Lucide icon IDs. */
export const GRAPH_LASSO_ICON_ID = "lasso";

/** Kept for backwards compatibility with existing imports. */
export function registerGraphLassoIcon(): void {}
