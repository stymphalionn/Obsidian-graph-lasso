# Obsidian-graph-lasso
Graph lasso plugin with buttons. allows a user to select files directly in graph view and perform actions wiht a right click. Extends Obsidian's graph view with minimal invasiveness
